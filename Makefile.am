#
#
#
AUTOMAKE_OPTIONS = 1.10 foreign no-define no-dist no-installinfo -Wall -Werror
SUBDIRS=

DAPS_CATALOG := for-catalog-$(PACKAGE).xml
DAPSROOT     := $(shell pwd)
DIRECTORIES  := catalogs

DESTDIR      ?=

STYLEROOT    := $(datadir)/xml/docbook/stylesheet/suse

#include Makefile.daps